# Timecell Internship Assignment

## Introduction

This project is part of the Timecell internship assessment.

---

## Local Secret Setup

For local runs, create a `.env` file with your API keys and keep it out of version control.

```bash
GEMINI_API_KEY=your_key_here
GOOGLE_API_KEY=your_key_here
```

The scripts read keys from environment variables at runtime, so the source code stays clean and the secrets stay local.

---

## Task 1: Portfolio Risk Analysis

LLM Used: Claude Opus 4.6
Prompt Source: Generated using ChatGPT

My Prompt

Generate a prompt that I can give to my copilot (Claud opes 4.6) to generate code for task 1, Requirements are: Clean code with proper comments, I want that Asset is a class with all necessary attributes needed ( think like a financial expert ), also portfolio is a class with necessary attributes and make five methods (functions) in it to generate five requirements related to portfolio, handle every possible error and edge cases ( like total distribution should be 100, divide by zero, crash percentage > -100, etc.....), dynamic code( like add, remove assets, can change attributes) and other basic requirements. (don't try to implement extra features or functions other than bonus question, don't implement methods (functions for just multiplication or division (unnecessary )). show output for only one input portfolio that is given : 
portfolio = { "total_value_inr": 10_000_000, # 1 Crore INR 
              "monthly_expenses_inr": 80_000, 
              "assets": [
                        {"name": "BTC", "allocation_pct": 30, "expected_crash_pct": -80}, 
                        {"name": "NIFTY50","allocation_pct": 40, "expected_crash_pct": -40}, 
                        {"name": "GOLD", "allocation_pct": 20, "expected_crash_pct": -15}, 
                        {"name": "CASH", "allocation_pct": 10, "expected_crash_pct": 0}, 
                        ] 
            }
            
---
**Prompt Generated by ChatGPT**  
You are a senior Python engineer with strong experience in fintech systems and risk modeling.

Write clean, production-quality Python 3.10+ code for **Task 01: Portfolio Risk Calculator**.

## 🎯 Objective

Design a system using **object-oriented programming** to compute portfolio risk metrics.

---

## 🧱 Architecture Requirements

### 1. Asset Class

Create an `Asset` class that represents a financial asset.

Attributes (at minimum):

* name (str)
* allocation_pct (float)
* expected_crash_pct (float)

Think like a **financial expert** — ensure attributes are validated and meaningful.

Include:

* Proper validation in constructor
* Helpful `__repr__` or `__str__`
* Any minimal helper methods ONLY if truly necessary (avoid over-engineering)

---

### 2. Portfolio Class

Create a `Portfolio` class.

Attributes:

* total_value_inr (float)
* monthly_expenses_inr (float)
* assets (list of Asset objects)

---

## ⚙️ Required Methods (STRICT: exactly 5 core methods)

Implement ONLY these 5 methods inside Portfolio:

1. `compute_post_crash_value()`
2. `compute_runway_months()`
3. `ruin_test()` → returns 'PASS' or 'FAIL'
4. `largest_risk_asset()` → based on (allocation_pct × absolute crash impact)
5. `concentration_warning()` → True if any asset > 40%

---

## 🎯 Bonus (Optional but preferred)

* Add support for **moderate crash scenario** (50% of crash magnitude)
* CLI-based allocation visualization (simple text bars, no external libs)

---

## ⚠️ Strict Constraints

### Code Quality

* Clean, readable, modular
* Proper docstrings and comments
* No unnecessary helper functions (e.g., don’t create functions just for multiplication/division)
* Follow Python best practices

---

### Error Handling & Edge Cases (VERY IMPORTANT)

Handle ALL of the following:

* Total allocation must equal 100%
* Allocation cannot be negative
* Crash percentage must be between -100 and 0
* Monthly expenses cannot be zero or negative
* Total portfolio value must be positive
* Division by zero
* Empty asset list
* Duplicate asset names (handle or warn)
* Invalid data types

Raise meaningful exceptions with clear messages.

---

### Dynamic Behavior

* Ability to:

  * Add assets
  * Remove assets
  * Update asset attributes safely
* Ensure validations still hold after modifications

---

## 📥 Input (use ONLY this for demonstration)

portfolio_data = {
"total_value_inr": 10_000_000,
"monthly_expenses_inr": 80_000,
"assets": [
{"name": "BTC", "allocation_pct": 30, "expected_crash_pct": -80},
{"name": "NIFTY50", "allocation_pct": 40, "expected_crash_pct": -40},
{"name": "GOLD", "allocation_pct": 20, "expected_crash_pct": -15},
{"name": "CASH", "allocation_pct": 10, "expected_crash_pct": 0},
]
}

---

## 🖥 Output Requirements

* Instantiate Portfolio using above input

* Print results clearly:

  * Post crash value
  * Runway months
  * Ruin test result
  * Largest risk asset
  * Concentration warning

* If bonus implemented:

  * Show both crash scenarios side-by-side

---

## 🚫 Do NOT do these

* Do NOT use external libraries
* Do NOT over-engineer
* Do NOT add unrelated features
* Do NOT hardcode outputs

---

## 🎯 Final Goal

The code should look like something you would confidently:

* Submit in a real internship
* Walk through line-by-line in an interview

Focus on correctness, clarity, and robustness over cleverness.

---

### Objective

To calculate how a portfolio performs under a crash scenario and determine whether the user can financially survive.

---

## Approach

The solution follows these steps:

1. **Calculate Post-Crash Value**

   * Each asset is reduced based on its crash percentage
   * Formula used:

     ```
     asset_value = total_value × allocation × (1 + crash%)
     ```
   * All asset values are summed to get final portfolio value

2. **Calculate Runway**

   * Runway shows how many months the user can survive
   * Formula:

     ```
     runway = post_crash_value / monthly_expenses
     ```

3. **Ruin Test**

   * If runway > 12 → PASS (safe)
   * Else → FAIL (risky)

4. **Identify Highest Risk Asset**

   * Based on:

     ```
     risk = allocation × crash%
     ```
   * Asset with highest value is considered most risky

5. **Check Concentration Risk**

   * If any asset > 40% → flagged as risky

---

## Thought Process

* Used object-oriented design with `Asset` and `Portfolio` classes to keep the code modular and close to real-world financial modeling
* Every input is validated at construction time — type checks, range checks, and aggregate constraints (allocation sum = 100%) — so invalid portfolios can never be created
* Dynamic operations (`add_asset`, `remove_asset`, `update_asset`) re-run validations after every change, with automatic rollback on failure to keep the portfolio in a consistent state
* Kept exactly 5 core methods as required, with a `severity` parameter to support both full and moderate crash scenarios without duplicating logic
* Focused on clear, readable formulas that map directly to financial concepts, making the code easy to walk through in an interview

---

## Edge Cases Handled

* Total allocation not equal to 100% (raises error with current total)
* Negative allocation percentages
* Crash percentage outside the -100 to 0 range
* Zero or negative monthly expenses (prevents division issues)
* Zero or negative portfolio value
* Empty asset list (portfolio must have at least one asset)
* Duplicate asset names (prints a warning)
* Invalid data types (e.g., string passed instead of float)
* Removing the last asset from a portfolio (prevented with error)
* Failed attribute updates are rolled back to maintain consistency

---

## Example Output

```
==============================================================
        PORTFOLIO RISK CALCULATOR — CRASH ANALYSIS
==============================================================
  Portfolio Value : ₹ 10,000,000.00
  Monthly Expenses: ₹     80,000.00
--------------------------------------------------------------

  📊 Asset Allocation

  BTC      │ ████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │  30.0%
  NIFTY50  │ ████████████████░░░░░░░░░░░░░░░░░░░░░░░░ │  40.0%
  GOLD     │ ████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │  20.0%
  CASH     │ ████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │  10.0%

==============================================================
  CRASH SCENARIO COMPARISON
==============================================================
  Metric                      │ Full Crash (100%)│ Moderate Crash (50%)
  ──────────────────────────────────────────────────────────
  Post-Crash Value (₹)        │ ₹    5,700,000 │ ₹    7,850,000
  Runway (months)             │           71.2 │           98.1
  Ruin Test                   │           PASS │           PASS
  ──────────────────────────────────────────────────────────

  🔴 Largest Risk Asset : BTC (score = 30.0% × |-80.0%| = 2400)
  ⚠️  Concentration Risk : No

==============================================================
```
---

## Key Learnings

* Importance of diversification
* Understanding portfolio risk
* Translating real-world finance into code
* Handling edge cases properly

---

## Task 2: Live Market Data Fetch

LLM Used: Claude Opus 4.6
Prompt Source: Generated using ChatGPT

My Prompt

Generate a pompt for Claud Oper 4.6 to generate code for task2. Requirements are: clean code with proper comments, do every necessary things for error handling and data validation( like parsing, structured data formate, error handling, invalid data handling, network related issues handled, user readable,show error and data in structured manner etc.....(like all other things))

---
**Prompt Generated by ChatGPT**

You are a senior Python engineer experienced in building reliable data pipelines and API integrations in fintech systems.

Write clean, production-quality Python 3.10+ code for **Task 02: Live Market Data Fetch**.

Build a robust Python script that fetches **live market data for 3+ assets** and displays them in a clean, structured terminal output.

**Functional Requirements:**
* Fetch prices for at least 3 assets (1 stock/index + 1 cryptocurrency minimum)
* Use only free public APIs (no paid keys)
* Print a clean, formatted table with: asset name, current price, currency, timestamp
* Handle all errors gracefully, if one asset fails, log the error and continue

**Error Handling & Data Validation:**
* API request failures (timeouts, connection errors)
* Invalid or empty API responses, rate limits
* Missing fields, unexpected data formats, incorrect types
* Fault tolerance, never crash the script

**Code Structure:**
* Modular: separate functions for fetching, parsing/validation, and display
* Proper docstrings, type hints, structured logging

---

### Objective

To fetch live market prices from free public APIs and display them in a clean, reliable terminal table, handling network failures and bad data gracefully.

---

### APIs Used

| API | Used For | Key Required |
|-----|----------|--------------|
| CoinGecko | BTC, ETH (crypto) | No |
| Yahoo Finance (v8 chart) | NIFTY 50, GOLD (stocks/commodities) | No |

Both accessed via Python's built-in `urllib`, no external libraries needed.

---

### Architecture

The code is split into three clean layers:

1. **Data Model** — `AssetPrice` dataclass holds the result (name, price, currency, timestamp, error)
2. **Fetchers** — `fetch_coingecko()` and `fetch_yahoo()` handle API calls + response validation
3. **Display** — `display_results()` renders an aligned table, showing errors inline for failed assets

```
Asset Registry → fetch_all_assets() → [AssetPrice, ...] → display_results()
                      │                                          │
              fetch_coingecko()                          Formatted Table
              fetch_yahoo()
```

---

### Error Handling

| Scenario | How It's Handled |
|----------|-----------------|
| API timeout (>10s) | `URLError` caught, logged, asset marked as failed |
| HTTP 429 (rate limit) | Specific message: "Rate limit exceeded — try again later" |
| HTTP 403 (blocked) | Specific message: "Access denied — Yahoo may be blocking" |
| Empty/invalid JSON | `JSONDecodeError` caught, logged as "Data error" |
| Missing price field | `ValueError` raised and caught during parsing |
| Wrong data type | `TypeError` caught (e.g., string instead of float) |
| One asset fails | Error logged, remaining assets continue fetching |
| All assets fail | Table still renders with error messages, no crash |

---

### Thought Process

* Used only Python standard library (`urllib`, `json`, `dataclasses`) to avoid dependency issues
* Each API has its own fetcher function with dedicated response validation — no shared parsing logic that could break both
* The `AssetPrice` dataclass provides a clean contract between fetching and display layers
* A simple asset registry (list of dicts) makes adding new assets a one-line change
* 10-second timeout prevents the script from hanging on unresponsive APIs

---

### Example Output

```
============================================================
       LIVE MARKET DATA FETCH — Task 02
============================================================

  Asset Prices — fetched at 2026-05-02 00:57:02 IST
  +----------+----------------+----------+---------------------------+
  | Asset    |          Price | Currency | Fetched At                |
  +----------+----------------+----------+---------------------------+
  | BTC      |      78,390.00 | USD      | 2026-05-02 00:57:01 IST   |
  | ETH      |       2,304.72 | USD      | 2026-05-02 00:57:01 IST   |
  | NIFTY 50 |      23,997.55 | INR      | 2026-05-02 00:57:01 IST   |
  | GOLD     |       4,638.90 | USD      | 2026-05-02 00:57:02 IST   |
  +----------+----------------+----------+---------------------------+

  Fetched: 4/4 assets successfully
```

---

### Key Learnings

* Importance of fault-tolerant design in data pipelines
* Separating data fetching, validation, and presentation layers
* Handling real-world API quirks (rate limits, missing fields, timeouts)
* Building extensible systems with a simple registry pattern


---

## Task 3: AI-Powered Portfolio Explainer

LLM Used: Claude Opus 4.6
Prompt Source: Generated using ChatGPT
API Used: Google Gemini (gemini-1.5-flash)

My Prompt

Generate a prompt for Claud opus 4.6 to do this task. Requirements: Clean and professional code with proper comments, preferred API is Gemini, able to take input as a portfolio, and the prompt inside the code should be role-based. The role is an honest and expert financial adviser with around 20 years of work experience. Also ask for a well structured output format that can be extracted easily to show, and provide necessary suggestions in simple language that can be understood by non expert persons. Handle every possible error and validation problem efficiently. In this version, the portfolio only provides asset names and allocation_pct, but current prices should be fetched from yfinance first, and the LLM should only explain the portfolio risk using those actual prices. In terminal it should show the asset names, fetched current prices, individual risks, overall risks, portfolio performance, and suggestions.

---
**Prompt Generated by ChatGPT**  
You are a senior Python engineer and AI systems developer with experience in fintech products and LLM integrations.

Write clean, production-quality Python 3.10+ code for Task 03: AI-Powered Portfolio Explainer.

### 🎯 Objective

Build a Python script that fetches current asset prices from yfinance and then uses Gemini to generate a plain-English explanation of portfolio risk, along with structured insights and actionable suggestions.

### 📥 Input Requirements

The script should accept a portfolio input dynamically (not hardcoded), with structure:

```python
portfolio = {
    "assets": [
        {"name": "BTC", "allocation_pct": 30},
        {"name": "NIFTY50", "allocation_pct": 40},
        {"name": "GOLD", "allocation_pct": 20},
        {"name": "CASH", "allocation_pct": 10},
    ]
}
```

Note: Portfolio does NOT include prices or risk data. Prices are fetched from yfinance, and the LLM uses those fetched prices only for explanation.

### 🤖 LLM Integration

* Use Google Gemini API
* Keep API key secure (use environment variables, with a fallback key in the script)
* Separate: prompt construction, API call, response parsing

### 🧠 Prompt Engineering

* Role: "Honest, highly experienced financial advisor with 20+ years of experience"
* Tone: Friendly but honest, simple language, avoid jargon
* Output: Strict JSON format for easy parsing
* Input to the model includes the fetched current prices, so it does not need to estimate prices itself

### 🔥 Required JSON Output Format

```json
{
  "summary": "...",
  "good_practice": "...",
  "improvement_suggestion": "...",
  "verdict": "...",
  "asset_analysis": [
    {
      "name": "BTC",
      "estimated_risk": "...",
      "reason": "...",
      "approx_price": "..."
    }
  ]
}
```

### ⚠️ Error Handling

Handle: input validation, yfinance price lookup failures, API errors, network failures, timeout errors, rate limits, invalid JSON, missing fields, malformed responses.

### 🎯 Bonus

* Configurable tone: beginner / experienced / expert
* Self-critique: Second LLM call to audit the first response

---

### Objective

To fetch real market prices first, then use AI (Google Gemini) to analyze a portfolio allocation and generate plain-English explanations of risk, along with structured insights and actionable suggestions.

---

## Approach

The solution follows these steps:

1. **Portfolio Input**

   * User can choose default sample portfolio or enter manually
   * Input is validated for: non-empty, no negatives, no duplicates, allocation sums to ~100%

2. **Price Fetching**

  * Current asset prices are fetched from yfinance
  * Common aliases like BTC, NIFTY50, and GOLD are mapped to Yahoo Finance tickers
  * Cash is treated as a stable zero-price asset equivalent

3. **Prompt Construction**

   * Role-based prompt: "honest, highly experienced financial advisor with 20+ years experience"
   * Tone-adaptive instructions (beginner/experienced/expert)
   * Strict JSON schema enforced in the prompt itself
  * Fetched prices are included in the prompt, so the LLM only explains risk instead of estimating prices

4. **API Call**

  * Calls Google Gemini API (`gemini-1.5-flash`) with low temperature (0.3) for consistency
   * Implements retry logic with exponential backoff (2s, 4s, 8s) for transient failures
   * Handles rate limits, timeouts, network errors, and blocked responses

5. **Response Parsing**

   * Extracts JSON from raw LLM output (handles markdown fences and extra text)
   * Validates all required fields: summary, good_practice, improvement_suggestion, verdict, asset_analysis
  * Provides fallback values for missing asset fields
  * Replaces any model-returned price text with the fetched yfinance price values before display

6. **Display Output**

  * Shows raw API response for transparency
   * Displays structured output with clean formatting and word wrapping
  * Asset-wise analysis: name, fetched current price, estimated risk, reason
   * Portfolio-level insights: summary, good practice, improvement suggestion, verdict

7. **Self-Critique (Bonus)**

   * Optional second LLM call acts as a "senior portfolio risk auditor"
   * Reviews first response for accuracy, price estimates, verdict justification
   * Returns accuracy score, corrections, and additional insights

---

## Thought Process

* Used a modular design with five clearly separated functions (`get_portfolio_input`, `build_prompt`, `call_gemini_api`, `parse_response`, `display_output`) following single-responsibility principle
* Added a separate yfinance price-fetch step so current prices are factual and not guessed by the LLM
* The prompt is carefully engineered with role definition, tone instructions, task breakdown, and strict JSON schema, this ensures consistent, parseable responses from the LLM
* Tone is configurable (beginner/experienced/expert) to adapt explanations for different audiences without changing the core logic
* JSON extraction logic handles common LLM quirks: markdown code fences, preamble text, and boundary detection, making parsing robust
* Self-critique feature sends the first response back to the LLM as a second "auditor" role, catching potential inaccuracies without any human intervention
* API key is read from environment variables first (`GEMINI_API_KEY` / `GOOGLE_API_KEY`), falling back to a built-in key so the demo works out of the box
* Every error path (input validation, API failures, parsing failures) produces clear, actionable error messages and exits cleanly

---

## Code Structure

```
task3.py
├── Custom Exceptions
│   ├── PortfolioInputError
│   ├── APIError
│   └── ResponseParsingError
├── get_portfolio_input()      → Interactive CLI input with validation
├── get_default_portfolio()    → Sample portfolio for quick demo
├── validate_portfolio()       → Structure & data integrity checks
├── fetch_current_prices()     → yfinance price lookup for each asset
├── resolve_price_symbol()     → Map asset names to Yahoo Finance tickers
├── build_prompt()             → Prompt engineering with tone config
├── build_critique_prompt()    → Second prompt for self-audit
├── configure_api()            → API key setup (env var priority)
├── call_gemini_api()          → API call with retry + backoff
├── extract_json_from_text()   → JSON extraction from raw LLM output
├── parse_response()           → JSON validation + missing field fallback
├── parse_critique_response()  → Critique parsing (non-critical)
├── display_raw_response()     → Raw API output for debugging
├── display_output()           → Formatted structured output
├── display_critique()         → Formatted critique results
└── main()                     → Orchestration pipeline
```

---

## Edge Cases Handled

* Empty portfolio (must contain at least one asset)
* Negative allocation percentages
* Allocation sum not equal to ~100% (1% tolerance)
* Duplicate asset names (case-insensitive detection)
* Missing required fields in portfolio dict
* Invalid data types (non-numeric allocation, non-string name)
* Invalid or unavailable yfinance prices (falls back to `N/A`)
* API rate limits / 429 errors (retried with exponential backoff)
* Network timeouts and connection failures
* Gemini safety filter blocks (empty candidates)
* Empty API response
* Malformed JSON from LLM (code fences, extra text stripped)
* Missing fields in LLM response (fallback to "N/A")
* Invalid verdict values (soft correction attempted)

---

## Example Output

```
==============================================================
        📊 PORTFOLIO ANALYSIS — STRUCTURED OUTPUT
==============================================================

  ┌─────────────────────────────────────────────────────┐
  │              ASSET-WISE ANALYSIS                    │
  └─────────────────────────────────────────────────────┘

  ──────────────────────────────────────────────────────
  │ Asset          : BTC
  │ Approx Price   : $65,000
  │ Estimated Risk : Very High
  │ Reason         : Bitcoin is extremely volatile and can
                    experience large price swings in short
                    periods.
  ──────────────────────────────────────────────────────
  │ Asset          : NIFTY50
  │ Approx Price   : ₹22,000
  │ Estimated Risk : High
  │ Reason         : NIFTY50 represents stock market
                    performance, which can fluctuate
                    significantly based on economic
                    conditions.
  ──────────────────────────────────────────────────────
  │ Asset          : GOLD
  │ Approx Price   : ₹6,500 per gram
  │ Estimated Risk : Medium
  │ Reason         : Gold is generally stable and often
                    performs well during uncertain times,
                    but it still has some price variation.
  ──────────────────────────────────────────────────────
  │ Asset          : CASH
  │ Approx Price   : ₹1 (stable value)
  │ Estimated Risk : Low
  │ Reason         : Cash does not fluctuate in value but
                    may lose purchasing power due to
                    inflation.
  ──────────────────────────────────────────────────────

  ┌─────────────────────────────────────────────────────┐
  │              PORTFOLIO-LEVEL INSIGHTS               │
  └─────────────────────────────────────────────────────┘

  📝 Summary:
     Your investment portfolio has a high level of risk
     because a large portion of your money is invested
     in Bitcoin and stock markets, which can go up and
     down quickly. While these investments can give high
     returns, they can also lose a lot of value in a
     short time. A smaller part of your money is in gold
     and cash, which are more stable and safer. Overall,
     your portfolio is tilted toward growth but comes
     with significant ups and downs.

  ✅ Good Practice:
     You have wisely kept some money in gold and cash,
     which helps protect your portfolio during market
     downturns and gives you financial safety when
     markets are unstable.

  💡 Improvement Suggestion:
     You should consider reducing a small portion of
     your investment from high-risk assets like Bitcoin
     and shifting it into safer options such as bonds
     or fixed deposits. This will make your portfolio
     more stable and reduce the chances of large losses
     during market crashes.

  🔴 Verdict: Aggressive

==============================================================
```

---

## Key Learnings

* Importance of prompt engineering for consistent, structured LLM output
* Using strict JSON schemas in prompts to enable reliable parsing
* Handling real-world API issues (rate limits, timeouts) with retry logic
* Separating concerns: input → prompt → API → parse → display
* Making AI output accessible to non-expert audiences through tone configuration
* Self-critique pattern: using a second LLM call to validate the first response

---

## Task 4: Portfolio Correlation Risk Analyzer

LLM Used: Gemini API
Prompt Source: Generated using ChatGPT

My Idea

Build a simple portfolio risk tool that looks beyond individual prices and asks a more important question: how are the assets behaving together?

If two or more assets keep moving in the same direction, then the portfolio is not truly diversified, even if the assets look different on the surface. That is the main idea behind this task. Instead of only tracking whether prices go up or down, the script measures correlation over time to see whether the portfolio is quietly becoming more dependent on a single market mood.

The tool also checks whether this relationship is getting stronger recently compared to the longer past. If short-term correlation is rising, it can mean diversification is weakening and hidden beta is increasing. In simple terms, the portfolio may be starting to behave like one big risk block instead of a group of separate assets.

The final goal is to turn this into something a non-expert can understand quickly: whether the portfolio is calm, whether risk is building, and whether actions like rebalancing, hedging, or holding more cash might make sense.

--- 
**Prompt Generated by ChatGPT**  
You are a senior Python engineer with expertise in quantitative finance, time-series analysis, and LLM integrations.

Write clean, production-quality Python 3.10+ code to implement a **Portfolio Correlation Risk Analyzer**.

---

## 🎯 Objective

Build a system that analyzes how assets in a portfolio move together over time (correlation), detects weakening diversification, and provides human-readable risk insights using an LLM.

---

## 📥 Input

* A list of asset tickers (e.g., NIFTY, BTC, GOLD)
* Fetch historical price data dynamically using `yfinance`

---

## 📦 Functional Requirements

### 1. Data Handling

* Fetch historical price data (2 years recommended)
* Convert prices to **daily returns**
* Handle:

  * Missing values
  * Misaligned timestamps
  * Invalid tickers

---

### 2. Correlation Analysis

#### Rolling Correlation

* Compute rolling correlation (e.g., 30-day window)
* Analyze correlation trend over time

#### Correlation Drift

* Compare:

  * Short-term average (last 30 days)
  * Long-term average (last 60 days)
* Detect if correlation is increasing

👉 Use proper statistical logic (not naive comparison):

* Trend slope OR
* Moving average comparison

---

### 3. Diversification Score

* Compute correlation matrix
* Use **mean absolute off-diagonal correlation**
* Convert into score (0–100)

---

### 4. Forecasting (AR(1))

* Use `statsmodels` to forecast next-step correlation
* Keep implementation simple and interpretable

---

### 5. Market Stress Signal (VIX)

* Fetch VIX data
* Classify risk:

  * LOW / MODERATE / HIGH

---

## 🤖 LLM Integration (VERY IMPORTANT)

Use **Google Gemini API (preferred)**.

### Prompt Requirements

Construct a prompt that:

* Assigns role:

  > "You are an expert financial advisor with 20+ years of experience"

* Input to LLM:

  * Correlation trends
  * Drift values
  * Forecast output
  * Diversification score
  * VIX level

---

### LLM Output Requirements

Ask LLM to generate:

* Simple explanation of portfolio risk
* Whether diversification is weakening
* Key insight (1–2 lines)
* Suggested actions:

  * Rebalancing
  * Hedging
  * Cash allocation

👉 Must be:

* Human-readable
* Simple (non-technical language)

---

### Structured Output Format (IMPORTANT)

Force LLM to return JSON:

```json
{
  "summary": "...",
  "risk_level": "...",
  "key_insight": "...",
  "suggestions": ["...", "..."]
}
```

---

## 🖥 Output (Terminal)

Display:

### Raw Data:

* Correlation (current)
* Drift
* Forecast
* Diversification score
* VIX level

### LLM Insights:

* Summary
* Risk level
* Suggestions

Format output cleanly and professionally.

---

## ⚠️ Error Handling

Handle ALL:

* API failures (yfinance / Gemini)
* Invalid tickers
* Missing data
* Empty datasets
* JSON parsing errors (LLM response)

---

## 🧱 Code Structure

Organize code into clear modules/functions:

* fetch_data()
* compute_returns()
* rolling_correlation()
* compute_drift()
* diversification_score()
* forecast_ar1()
* vix_signal()
* build_llm_prompt()
* call_llm()
* display_results()

Use:

* Type hints
* Docstrings
* Clean modular design

---

## ⚡ Optimization

* Avoid redundant calculations
* Use vectorized pandas operations
* Keep code efficient and readable

---

## 🚫 Constraints

* Do NOT over-engineer
* Do NOT use unnecessary ML models
* Keep implementation simple but meaningful

---

## 🎯 Final Goal

The code should:

* Demonstrate understanding of financial risk concepts
* Be clean, modular, and production-ready
* Provide meaningful insights (not just numbers)

Focus on **clarity, correctness, and practical usefulness**.


---

---

### Objective

To analyze correlation risk in a portfolio, detect weakening diversification, forecast whether correlations may stay high, and show a simple market stress signal using VIX.

---

### Approach

The solution follows these steps:

1. **Fetch Market Data**

  * Download historical prices using `yfinance`
  * Handle missing values, invalid tickers, and misaligned dates

2. **Convert Prices to Returns**

  * Daily returns are calculated from prices
  * Returns are used instead of raw prices because correlation analysis works better on returns

3. **Measure Correlation**

  * Compute rolling correlation over a 30-day window
  * Track how correlation changes over time instead of assuming it is constant

4. **Detect Correlation Drift**

  * Compare short-term and long-term average correlation
  * If short-term correlation is higher, diversification may be weakening

5. **Score Diversification**

  * Use mean absolute off-diagonal correlation
  * Convert it into a simple 0–100 diversification score

6. **Forecast Correlation**

  * Use a simple AR(1) time-series model
  * Predict whether correlation is likely to stay high

7. **Check Market Stress**

  * Use VIX as a market fear / stress indicator
  * Classify it as LOW, MODERATE, or HIGH

8. **Ask Gemini for a Simple Explanation**

  * Send the computed metrics to Gemini
  * Ask for a short, human-readable summary in JSON format
  * If Gemini fails, the script still shows the raw analysis metrics

---

### Thought Process

* Focused on correlation instead of just price movement because diversification risk is about how assets behave together
* Used rolling windows so the analysis shows changes over time instead of a single static correlation value
* Kept the forecasting model simple with AR(1) so the result is easy to understand and explain
* Added VIX because market stress often increases correlation across assets
* Used a strict JSON prompt so the LLM response can be parsed reliably
* Kept the output human-friendly, so a non-technical person can understand whether the portfolio is becoming riskier

---

### Example Output

```
⚠  Gemini insight skipped: Gemini request failed. Try again or check the model/key.

========================================================================
PORTFOLIO CORRELATION RISK ANALYZER
========================================================================

Raw Data
------------------------------------------------------------------------
Tickers: NIFTY, BTC, GOLD
Current correlation: 2.35%
Drift: -1.32%
Short-term average: 1.25%
Long-term average: 2.56%
AR(1) forecast: 2.37%
Diversification score: 99.94 / 100
VIX: 16.99 (MODERATE - Moderate market stress)

LLM Insights
------------------------------------------------------------------------
Gemini insights unavailable.
```

---

### Key Learnings

* Correlation is often a better risk signal than price alone
* Rolling analysis helps detect changing relationships between assets
* A simple forecasting model can be enough when the goal is clarity, not complexity
* LLMs are useful for turning numeric risk metrics into plain English explanations
* Good fallback behavior matters because APIs can fail even when the analysis logic is correct


