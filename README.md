# Timecell Internship Assignment

## Introduction

This project is part of the Timecell internship assessment.

---

## Task 1: Portfolio Risk Analysis

LLM Used: Claude Opus 4.6
Prompt Source: Generated using ChatGPT

My Prompt

Generate a prompt that I can give to my copilot (Claud opes 4.6) to generate code for task 1, Requirements are: Clean code with proper comments, I want that Asset is a class with all necessary attributes needed ( think like a financial expert ), also portfolio is a class with necessary attributes and make five methods (functions) in it to generate five requirements related to portfolio, handle every possible error and edge cases ( like total distribution should be 100, divide by zero, crash percentage > -100, etc.....), dynamic code( like add, remove assets, can change attributes) and other basic requirements. (don't try to implement extra features or functions other than bonus question, don't implement methods (functions for just multiplication or division (unnecessary )). show output for only one input portfolio that is given : 
portfolio = { "total_value_inr": 10_000_000, # 1 Crore INR 
              "monthly_expenses_inr": 80_000, 
              "assets": [
                        {"name": "BTC", "allocation_pct": 30, "expected_crash_pct": -80}, 
                        {"name": "NIFTY50","allocation_pct": 40, "expected_crash_pct": -40}, 
                        {"name": "GOLD", "allocation_pct": 20, "expected_crash_pct": -15}, 
                        {"name": "CASH", "allocation_pct": 10, "expected_crash_pct": 0}, 
                        ] 
            }
            
---
**Prompt Generated by ChatGPT**  
You are a senior Python engineer with strong experience in fintech systems and risk modeling.

Write clean, production-quality Python 3.10+ code for **Task 01: Portfolio Risk Calculator**.

## 🎯 Objective

Design a system using **object-oriented programming** to compute portfolio risk metrics.

---

## 🧱 Architecture Requirements

### 1. Asset Class

Create an `Asset` class that represents a financial asset.

Attributes (at minimum):

* name (str)
* allocation_pct (float)
* expected_crash_pct (float)

Think like a **financial expert** — ensure attributes are validated and meaningful.

Include:

* Proper validation in constructor
* Helpful `__repr__` or `__str__`
* Any minimal helper methods ONLY if truly necessary (avoid over-engineering)

---

### 2. Portfolio Class

Create a `Portfolio` class.

Attributes:

* total_value_inr (float)
* monthly_expenses_inr (float)
* assets (list of Asset objects)

---

## ⚙️ Required Methods (STRICT: exactly 5 core methods)

Implement ONLY these 5 methods inside Portfolio:

1. `compute_post_crash_value()`
2. `compute_runway_months()`
3. `ruin_test()` → returns 'PASS' or 'FAIL'
4. `largest_risk_asset()` → based on (allocation_pct × absolute crash impact)
5. `concentration_warning()` → True if any asset > 40%

---

## 🎯 Bonus (Optional but preferred)

* Add support for **moderate crash scenario** (50% of crash magnitude)
* CLI-based allocation visualization (simple text bars, no external libs)

---

## ⚠️ Strict Constraints

### Code Quality

* Clean, readable, modular
* Proper docstrings and comments
* No unnecessary helper functions (e.g., don’t create functions just for multiplication/division)
* Follow Python best practices

---

### Error Handling & Edge Cases (VERY IMPORTANT)

Handle ALL of the following:

* Total allocation must equal 100%
* Allocation cannot be negative
* Crash percentage must be between -100 and 0
* Monthly expenses cannot be zero or negative
* Total portfolio value must be positive
* Division by zero
* Empty asset list
* Duplicate asset names (handle or warn)
* Invalid data types

Raise meaningful exceptions with clear messages.

---

### Dynamic Behavior

* Ability to:

  * Add assets
  * Remove assets
  * Update asset attributes safely
* Ensure validations still hold after modifications

---

## 📥 Input (use ONLY this for demonstration)

portfolio_data = {
"total_value_inr": 10_000_000,
"monthly_expenses_inr": 80_000,
"assets": [
{"name": "BTC", "allocation_pct": 30, "expected_crash_pct": -80},
{"name": "NIFTY50", "allocation_pct": 40, "expected_crash_pct": -40},
{"name": "GOLD", "allocation_pct": 20, "expected_crash_pct": -15},
{"name": "CASH", "allocation_pct": 10, "expected_crash_pct": 0},
]
}

---

## 🖥 Output Requirements

* Instantiate Portfolio using above input

* Print results clearly:

  * Post crash value
  * Runway months
  * Ruin test result
  * Largest risk asset
  * Concentration warning

* If bonus implemented:

  * Show both crash scenarios side-by-side

---

## 🚫 Do NOT do these

* Do NOT use external libraries
* Do NOT over-engineer
* Do NOT add unrelated features
* Do NOT hardcode outputs

---

## 🎯 Final Goal

The code should look like something you would confidently:

* Submit in a real internship
* Walk through line-by-line in an interview

Focus on correctness, clarity, and robustness over cleverness.

---

### Objective

To calculate how a portfolio performs under a crash scenario and determine whether the user can financially survive.

---

## Approach

The solution follows these steps:

1. **Calculate Post-Crash Value**

   * Each asset is reduced based on its crash percentage
   * Formula used:

     ```
     asset_value = total_value × allocation × (1 + crash%)
     ```
   * All asset values are summed to get final portfolio value

2. **Calculate Runway**

   * Runway shows how many months the user can survive
   * Formula:

     ```
     runway = post_crash_value / monthly_expenses
     ```

3. **Ruin Test**

   * If runway > 12 → PASS (safe)
   * Else → FAIL (risky)

4. **Identify Highest Risk Asset**

   * Based on:

     ```
     risk = allocation × crash%
     ```
   * Asset with highest value is considered most risky

5. **Check Concentration Risk**

   * If any asset > 40% → flagged as risky

---

## Thought Process

* Used object-oriented design with `Asset` and `Portfolio` classes to keep the code modular and close to real-world financial modeling
* Every input is validated at construction time — type checks, range checks, and aggregate constraints (allocation sum = 100%) — so invalid portfolios can never be created
* Dynamic operations (`add_asset`, `remove_asset`, `update_asset`) re-run validations after every change, with automatic rollback on failure to keep the portfolio in a consistent state
* Kept exactly 5 core methods as required, with a `severity` parameter to support both full and moderate crash scenarios without duplicating logic
* Focused on clear, readable formulas that map directly to financial concepts, making the code easy to walk through in an interview

---

## Edge Cases Handled

* Total allocation not equal to 100% (raises error with current total)
* Negative allocation percentages
* Crash percentage outside the -100 to 0 range
* Zero or negative monthly expenses (prevents division issues)
* Zero or negative portfolio value
* Empty asset list (portfolio must have at least one asset)
* Duplicate asset names (prints a warning)
* Invalid data types (e.g., string passed instead of float)
* Removing the last asset from a portfolio (prevented with error)
* Failed attribute updates are rolled back to maintain consistency

---

## Example Output

```
==============================================================
        PORTFOLIO RISK CALCULATOR — CRASH ANALYSIS
==============================================================
  Portfolio Value : ₹ 10,000,000.00
  Monthly Expenses: ₹     80,000.00
--------------------------------------------------------------

  📊 Asset Allocation

  BTC      │ ████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │  30.0%
  NIFTY50  │ ████████████████░░░░░░░░░░░░░░░░░░░░░░░░ │  40.0%
  GOLD     │ ████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │  20.0%
  CASH     │ ████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │  10.0%

==============================================================
  CRASH SCENARIO COMPARISON
==============================================================
  Metric                      │ Full Crash (100%)│ Moderate Crash (50%)
  ──────────────────────────────────────────────────────────
  Post-Crash Value (₹)        │ ₹    5,700,000 │ ₹    7,850,000
  Runway (months)             │           71.2 │           98.1
  Ruin Test                   │           PASS │           PASS
  ──────────────────────────────────────────────────────────

  🔴 Largest Risk Asset : BTC (score = 30.0% × |-80.0%| = 2400)
  ⚠️  Concentration Risk : No

==============================================================
```
---

## Key Learnings

* Importance of diversification
* Understanding portfolio risk
* Translating real-world finance into code
* Handling edge cases properly

---

## Task 2: Live Market Data Fetch

LLM Used: Claude Opus 4.6
Prompt Source: Generated using ChatGPT

My Prompt

Generate a pompt for Claud Oper 4.6 to generate code for task2. Requirements are: clean code with proper comments, do every necessary things for error handling and data validation( like parsing, structured data formate, error handling, invalid data handling, network related issues handled, user readable,show error and data in structured manner etc.....(like all other things))

---
**Prompt Generated by ChatGPT**

You are a senior Python engineer experienced in building reliable data pipelines and API integrations in fintech systems.

Write clean, production-quality Python 3.10+ code for **Task 02: Live Market Data Fetch**.

Build a robust Python script that fetches **live market data for 3+ assets** and displays them in a clean, structured terminal output.

**Functional Requirements:**
* Fetch prices for at least 3 assets (1 stock/index + 1 cryptocurrency minimum)
* Use only free public APIs (no paid keys)
* Print a clean, formatted table with: asset name, current price, currency, timestamp
* Handle all errors gracefully — if one asset fails, log the error and continue

**Error Handling & Data Validation:**
* API request failures (timeouts, connection errors)
* Invalid or empty API responses, rate limits
* Missing fields, unexpected data formats, incorrect types
* Fault tolerance — never crash the script

**Code Structure:**
* Modular: separate functions for fetching, parsing/validation, and display
* Proper docstrings, type hints, structured logging

---

### Objective

To fetch live market prices from free public APIs and display them in a clean, reliable terminal table — handling network failures and bad data gracefully.

---

### APIs Used

| API | Used For | Key Required |
|-----|----------|--------------|
| CoinGecko | BTC, ETH (crypto) | No |
| Yahoo Finance (v8 chart) | NIFTY 50, GOLD (stocks/commodities) | No |

Both accessed via Python's built-in `urllib` — no external libraries needed.

---

### Architecture

The code is split into three clean layers:

1. **Data Model** — `AssetPrice` dataclass holds the result (name, price, currency, timestamp, error)
2. **Fetchers** — `fetch_coingecko()` and `fetch_yahoo()` handle API calls + response validation
3. **Display** — `display_results()` renders an aligned table, showing errors inline for failed assets

```
Asset Registry → fetch_all_assets() → [AssetPrice, ...] → display_results()
                      │                                          │
              fetch_coingecko()                          Formatted Table
              fetch_yahoo()
```

---

### Error Handling

| Scenario | How It's Handled |
|----------|-----------------|
| API timeout (>10s) | `URLError` caught, logged, asset marked as failed |
| HTTP 429 (rate limit) | Specific message: "Rate limit exceeded — try again later" |
| HTTP 403 (blocked) | Specific message: "Access denied — Yahoo may be blocking" |
| Empty/invalid JSON | `JSONDecodeError` caught, logged as "Data error" |
| Missing price field | `ValueError` raised and caught during parsing |
| Wrong data type | `TypeError` caught (e.g., string instead of float) |
| One asset fails | Error logged, remaining assets continue fetching |
| All assets fail | Table still renders with error messages, no crash |

---

### Thought Process

* Used only Python standard library (`urllib`, `json`, `dataclasses`) to avoid dependency issues
* Each API has its own fetcher function with dedicated response validation — no shared parsing logic that could break both
* The `AssetPrice` dataclass provides a clean contract between fetching and display layers
* A simple asset registry (list of dicts) makes adding new assets a one-line change
* 10-second timeout prevents the script from hanging on unresponsive APIs

---

### Example Output

```
============================================================
       LIVE MARKET DATA FETCH — Task 02
============================================================

  Asset Prices — fetched at 2026-05-02 00:57:02 IST
  +----------+----------------+----------+---------------------------+
  | Asset    |          Price | Currency | Fetched At                |
  +----------+----------------+----------+---------------------------+
  | BTC      |      78,390.00 | USD      | 2026-05-02 00:57:01 IST   |
  | ETH      |       2,304.72 | USD      | 2026-05-02 00:57:01 IST   |
  | NIFTY 50 |      23,997.55 | INR      | 2026-05-02 00:57:01 IST   |
  | GOLD     |       4,638.90 | USD      | 2026-05-02 00:57:02 IST   |
  +----------+----------------+----------+---------------------------+

  Fetched: 4/4 assets successfully
```

---

### Key Learnings

* Importance of fault-tolerant design in data pipelines
* Separating data fetching, validation, and presentation layers
* Handling real-world API quirks (rate limits, missing fields, timeouts)
* Building extensible systems with a simple registry pattern
